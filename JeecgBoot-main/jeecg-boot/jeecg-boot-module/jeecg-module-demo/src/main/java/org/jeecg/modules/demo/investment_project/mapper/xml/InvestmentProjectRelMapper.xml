<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.demo.investment_project.mapper.InvestmentProjectRelMapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE 
		FROM  investment_project_rel 
		WHERE
			 head_id = #{mainId}
	</delete>
	
	<select id="selectByMainId" parameterType="java.lang.String" resultType="org.jeecg.modules.demo.investment_project.entity.InvestmentProjectRel">
		SELECT * 
		FROM  investment_project_rel
		WHERE
			 head_id = #{mainId}
	</select>
</mapper>
